{
    "title": "service_tasks",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Primary key, auto-increment"
        },
        "service_progress_id": {
            "type": "integer",
            "description": "Associated service progress ID"
        },
        "customer_id": {
            "type": "integer",
            "description": "Associated customer ID"
        },
        "task_name": {
            "type": "string",
            "description": "Task name"
        },
        "task_type": {
            "type": "string",
            "description": "Task type: setup_group, collect_info, collect_images, collect_logo, confirm_service, open_facebook, open_instagram, bind_google, update_info, publish_content, reply_comments, submit_report, collect_menu, collect_price, input_menu, page_design, backend_setup, stripe_config, test_order, go_live, other"
        },
        "assignee_name": {
            "type": "string",
            "description": "Assignee name"
        },
        "priority": {
            "type": "string",
            "description": "Priority: high, medium, low"
        },
        "status": {
            "type": "string",
            "description": "Status: pending, in_progress, completed, delayed, waiting_client, cancelled"
        },
        "due_date": {
            "type": "string",
            "description": "Due date"
        },
        "completed_date": {
            "type": "string",
            "description": "Completed date"
        },
        "notes": {
            "type": "string",
            "description": "Task notes"
        },
        "user_id": {
            "type": "string",
            "description": "User ID"
        },
        "created_at": {
            "type": "string",
            "description": "Created timestamp"
        }
    },
    "required": [
        "id",
        "customer_id",
        "task_name",
        "status",
        "user_id"
    ]
}